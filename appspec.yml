version: 0.0
os: linux
files:
  -source: /index.html
   destination: /var/www/html/
   
hooks:
  BeforeInstall:
    -location: /install-dependencies
     timeout: 300
     runs: root
     
    -location: /startserver
     timeout: 300
     runs: root
     
